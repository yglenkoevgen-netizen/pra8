Виконані всі 5 вправ воркшопу `eu-node-basics-workshop`:
1. ROOT ROUTE
2. TIME ROUTE
3. ECHO ROUTE
4. SUM ROUTE
5. NOT FOUND ROUTE

Для перевірки роботи серверів локально, запустіть потрібний файл через node, вказавши порт (наприклад, 3000):
`node solutions/root_route.js 3000`